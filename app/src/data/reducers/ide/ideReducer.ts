import { IIde } from "../../api/ide/IIde";
import { IAction } from "../../api/IAction";

const initialState: IIde = {
}

export const ideReducer = (state: IIde = initialState, action: IAction) => {
  return state;
}

import { IHash } from "../../api/IHash";
import { IItem } from "../../api/project/IItem";
import { IAction } from "../../api/IAction";

export const itemsReducer = (state: IHash<IItem>, action: IAction) => {
  return state;
}

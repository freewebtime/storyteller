{
  "name": "story-script-extension",
  "displayName": "story-script-extension",
  "description": "language for making stories",
  "author": "Jack Sea",
  "license": "MIT",
  "version": "0.0.1",
  "publisher": "freewebtime",
  "repository": {
    "type": "git",
    "url": "https://github.com/freewebtime/storyteller.git"
  },
  "scripts": {
    "debug-babel-tutorial": "tsc -p ./babel_tutorial/tsconfig.json",
    "compile-babel": "tsc -p babel_plugin",
    "debug-babel": "tsc -p babel_plugin && babel test_story/src --out-dir test_story/.compiled --source-maps",
    "postinstall": "cd server && npm install && cd ../client && npm install && cd ..",
    "compile": "tsc -p client/tsconfig.json && cd server && npm run installServer && cd .. && tsc -p server/tsconfig.json && tsc -p babel_plugin",
    "compile:client": "tsc -p client/tsconfig.json && tsc -p babel_plugin",
    "watch:client": "tsc -w -p client/tsconfig.json",
    "compile:server": "cd server && npm run installServer && cd .. && tsc -p server/tsconfig.json",
    "watch:server": "cd server && npm run installServer && cd .. && tsc -w -p server/tsconfig.json"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.42",
    "@types/node": "^6.0.88",
    "babel-cli": "^6.26.0",
    "regexpu": "^4.1.3",
    "typescript": "^2.5.2"
  },
  "dependencies": {
    "babel-core": "^6.26.0"
  }
}

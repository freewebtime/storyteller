import { stsTokenizer } from "./tokenizing/stsTokenizer";
import { stsParser } from "./parsing/stsParser";
import { jsCompiler } from "./compilation/jsCompiler";

export const storyscript = {
  stsTokenizer,
  stsParser,
  jsCompiler,
}